{
  "name": "hackai2026",
  "version": "0.1.0",
  "description": "CometPath — UTD AI Degree Planner",
  "scripts": {
    "dev": "concurrently --names SERVER,CLIENT --prefix-colors cyan,magenta \"nodemon --cwd comet-path/server index.js\" \"npm run dev --prefix comet-path/client\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
